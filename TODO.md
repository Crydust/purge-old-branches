# TODO

* [ ] read ticket id and status from a csv file and store the ticket ids with status `Done` in some sort of collection
* [ ] read all git branches merged into a "master-ish" one (the name of the master branch must be configurable)
* [ ] read the commit date of each HEAD of each of those branches
* [ ] delete the branches
* [ ] make local or remote configurable
* [ ] make the prefix we filter branches on configurable
* [ ] the csv file contains the ticket id, but the branch name only start with a ticket id